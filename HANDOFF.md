# 핸드오프 노트 — 이탈리아 여행 2026 앱

이 문서는 이전 세션(Claude Code)에서 진행한 작업을 요약한 것입니다. 새 세션에서 이 레포를
GitHub에 연결해 이어서 진행할 수 있도록 정리했습니다.

## 배경

- 이 워크스페이스는 **Claude Design**(claude.ai/design)에서 내보낸(export) 핸드오프 번들입니다.
- `README.md`, `chats/chat1.md`, `chats/chat2.md`, `project/` 폴더가 원본 디자인 산출물입니다.
- `project/이탈리아 여행 2026.dc.html`이 구현해야 할 원본 디자인 파일이었습니다. 이 파일은
  Claude Design 전용 프로토타이핑 프레임워크(`sc-for`/`sc-if` 커스텀 태그 + `DCLogic` 컴포넌트,
  `support.js`가 런타임)로 작성되어 있어 그대로 배포할 수 없고, 실제로 동작하는 코드로
  재구현이 필요했습니다.

## chats/chat2.md 요약 (여행 일정 관리 앱 기획 과정)

사용자가 아내와 함께 떠나는 이탈리아 여행 일정 관리 앱을 요청하며 여러 차례 수정 요청을 거쳤습니다:

1. 최초 요청: 9/10 저녁 출발(EVA Air, 타이베이 경유) → 밀라노 → 토스카나 → 돌로미티 →
   베네치아 → 9/26 밀라노 출발 → 9/27 인천 도착. 기능: 날짜별 타임라인, 교통/숙소 정보,
   가볼 곳·맛집 체크리스트, 패킹 리스트, 예산 관리, 여권·예약번호 등 중요 정보 보관.
   대상 도시: 시에나, 산 지미냐노, 볼로냐, 베로나, 돌로미티, 베네치아. 한국어 + 이탈리아어/영어 지명 병기.
2. **출발일이 9/17로 변경**(귀국일 9/27은 유지) → 체류가 15박에서 8박으로 단축되어 6개 도시를
   압축한 빠듯한 일정으로 재구성.
3. 토스카나 와인 데이 상세 추가: D4(9/20)에 몬탈치노 브루넬로 와이너리 투어 → 몬테풀치아노 →
   볼로냐 이동, 구간별 소요시간·거리 포함.
4. 9/23 밤 **돌로미티 산장(리푸지오) 1박** 추가 — 코르티나 호텔 숙박과 별도로 분리.
5. 9/25 일정 변경: 베네치아 오전 관광 → 오후 늦게 산타루치아역에서 기차 탑승 → 밀라노
   Gallarate역 하차 → Jet Hotel 1박 → 9/26 아침 택시로 말펜사 공항.
6. 도시 간 이동에 **소요시간 + 거리(km)** 모두 표기.
7. 관광지·숙소에 **구글 지도 링크**, 기차 구간에 **Trainline 예매 링크** 추가.
8. 구글 지도가 iframe 미리보기 샌드박스에서 차단되는 문제 → `target="_blank"` +
   `window.open(..., "_blank", "noopener")`로 새 탭에서 강제로 열리도록 수정.
9. 마지막 요청: **핸드폰에서도 볼 수 있게 하나의 파일로 묶어 오프라인에서도 열리게** 해달라는 요청.

(chat1.md는 별개 프로젝트인 경제뉴스 브리핑 슬라이드 — 이번 작업과 무관.)

## 이번 세션에서 한 일

`project/이탈리아 여행 2026.dc.html`을 분석한 뒤, 사용자에게 3가지를 확인하고 (AskUserQuestion):

- **출력 위치**: 레포 루트에 단일 파일 (권장안 선택됨)
- **D-day 카운트다운**: 실시간 계산 유지 (권장안 선택됨)
- **데이터 저장**: localStorage 자동 저장 유지 (권장안 선택됨)

→ **`italia-2026.html`**(레포 루트)을 순수 HTML/CSS/바닐라 JS 단일 파일로 새로 구현했습니다.
Claude Design의 `sc-for`/`sc-if`/`DCLogic` 프레임워크 의존성을 완전히 제거하고, 표준 DOM
이벤트 위임(delegated `click`/`change` 리스너) 방식으로 재작성했습니다. 서버·빌드 과정 없이
브라우저에서 파일을 직접 열면 바로 동작합니다(오프라인 지원, Pretendard 폰트만 CDN 필요하며
실패 시 시스템 폰트로 자연스럽게 폴백).

### 구현 내용

- **헤더**: 여행 타이틀, 실시간 D-day, 숙박 일수(예산 산정용 실제 숙박 박수 합계로 계산),
  여행 기간 pill, 항공사, 인원수
- **탭 7개**: 일정(timeline) · 교통(transport) · 숙소(stays) · 체크리스트(checklist) ·
  패킹(packing) · 예산(budget) · 정보(info)
- **일정 탭**: 9/17~9/27 전체 11일 타임라인, 날짜 칩 선택, 시간/제목/메모 인라인 편집,
  항목 추가/삭제. chat2.md에서 확정된 최종 일정(D4 와인 데이, D7 산장 1박, D9 Gallarate 경유
  등)을 그대로 시드 데이터로 반영.
- **교통 탭**: 편명/노선, 출발지→도착지, 날짜/시간, 예약번호, 기차는 Trainline 예매 버튼 노출
- **숙소 탭**: 체크인/아웃, 숙박 박수 자동 계산, 구글 지도 링크, 주소/예약번호
- **체크리스트 탭**: 가볼 곳/맛집 카테고리 토글, 도시별 그룹, 완료 체크, 구글 지도 링크
- **패킹 탭**: 진행률 바(%, 완료 개수), 카테고리별 그룹
- **예산 탭**: 총액(€/₩ 합산), 지불 완료/잔액, 항목별 통화 선택·금액 입력·지불 체크
- **정보 탭**: 여권번호 등 민감 정보는 기본 마스킹, "표시" 버튼으로 토글
- 모든 지도/예매 링크는 `target="_blank" rel="noopener"` 표준 앵커 태그 사용 (iframe 미사용,
  원본에서 겪었던 미리보기 샌드박스 차단 문제 자체가 발생하지 않음)
- 모든 편집 내용은 `localStorage`(`italia2026_trip_v1` 키)에 자동 저장

### 검증

Playwright 헤드리스 브라우저로 다음을 확인 완료:
- 7개 탭 모두 정상 렌더링
- 날짜 이동, 필드 편집 → localStorage에 정확히 반영
- 체크리스트/패킹 토글, 예산 합계 계산(€4,120 / ₩3,200,000) 정확
- 숙소 숙박 박수 계산 정확
- 정보 탭 비밀 필드 표시/숨김 토글 정상
- 구글 지도 링크 URL 정상 생성

**버그 수정 1건**: 숙박 일수를 처음에 `일정 일수 - 1`로 잘못 계산해 "10박"으로 표시되던 것을
발견, 실제 숙소 체크인/아웃 기준 합산으로 수정해 "8박 11일"로 바로잡음 (chat2.md에서 사용자와
합의된 값과 일치).

### 스크린샷 확인

일정/체크리스트/예산/패킹 탭 스크린샷으로 원본 디자인과 색상·레이아웃(배경 #EFEAE1, 카드
#F7F4EE, 헤더 다크 #2B2824, 액센트 테라코타 #B0603A, 올리브 #6B7A55 등)이 일치함을 육안 확인.

## 현재 레포 상태

- **git remote 없음** — GitHub에 연결되어 있지 않은 로컬 전용 레포입니다.
- 커밋 히스토리: `3e3ed81 Claude Design handoff: 경제뉴스 슬라이드 제작` (원본 export 시 생성된 커밋, 이번 작업과 무관한 다른 프로젝트)
- `italia-2026.html`은 아직 **커밋되지 않은 untracked 파일**입니다.
- 이 환경(샌드박스)에는 `gh` CLI, SSH 키, GitHub 토큰이 전혀 설정되어 있지 않아 이 세션에서는
  레포 생성이나 push가 불가능했습니다. → 그래서 사용자가 GitHub 연결이 가능한 새 세션에서
  이어가기로 함.

## 새 세션에서 할 일 (제안)

1. 이 zip을 새 세션 작업 디렉터리에 풀기 (또는 그대로 레포로 사용)
2. `git add italia-2026.html && git commit -m "Add Italy trip 2026 itinerary app"`
3. GitHub에 새 레포 생성 후 원격 연결:
   ```
   gh repo create <repo-name> --private --source=. --remote=origin
   git push -u origin main
   ```
   (`gh` CLI 미사용 시 GitHub 웹에서 빈 레포 생성 → `git remote add origin <url>` → `git push -u origin main`)
4. 필요하면 GitHub Pages로 `italia-2026.html`을 바로 웹에서 열리게 배포 가능 (요청 시 안내)

## 파일 목록

```
README.md                          원본 Claude Design 핸드오프 안내
chats/chat1.md                     경제뉴스 브리핑 프로젝트 대화 (무관)
chats/chat2.md                     이탈리아 여행 앱 기획 전체 대화 (★ 핵심 컨텍스트)
project/이탈리아 여행 2026.dc.html   원본 디자인 소스 (Claude Design 프로토타입, 참고용)
project/support.js                 원본 디자인 런타임 (참고용, 최종 구현에는 미사용)
project/deck-stage.js              경제뉴스 프로젝트용 (무관)
project/경제뉴스 브리핑.dc.html      경제뉴스 프로젝트용 (무관)
italia-2026.html                   ★ 최종 구현물 — 단일 파일 여행 관리 앱
HANDOFF.md                         이 문서
```
